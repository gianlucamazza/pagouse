# Copy to ~/.config/pagouse/credentials.toml and keep permissions at 0600.
# This file contains references only; it never contains resolved secret values.

[credentials.example_login]
origin = "https://example.com"
username = "op://Pagouse/Example Login/username"
password = "op://Pagouse/Example Login/password"
